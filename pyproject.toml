[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "erpnext_wms"
authors = [{name = "ERPNext WMS", email = "support@erpnext-wms.com"}]
version = "1.0.0"
description = "Complete Warehouse Management System for ERPNext"
requires-python = ">=3.7"
dependencies = [
    "frappe>=16.0.0"
]
